- link:
    ~/.zshrc:
    ~/.tmux.conf:
    ~/.config/hypr:
      path: hypr/omarchy3
    ~/.config/ghostty:
    ~/.config/lazygit/config.yml:
      path: lazygit/config.yml
    ~/.config/waybar:
    ~/.config/omarchy:
      path: omarchy/omarchy3
    /etc/keyd/default.conf:
      # This one might have be to run with sudo
      path: "keyd/default.conf"
